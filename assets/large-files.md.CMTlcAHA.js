import{H as e,V as t,et as n,j as r}from"./chunks/framework.B_YhZOsv.js";var i=JSON.parse(`{"title":"Large File Uploads","description":"","frontmatter":{},"headers":[],"relativePath":"large-files.md","filePath":"large-files.md"}`),a={name:`large-files.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="large-file-uploads" tabindex="-1">Large File Uploads <a class="header-anchor" href="#large-file-uploads" aria-label="Permalink to “Large File Uploads”">​</a></h1><p>Chunking is the advanced upload path for files that should not be sent in one request. When enabled, <code>useCourier</code> splits a large file into smaller pieces and sends those pieces sequentially to your chunk endpoint.</p><h2 id="when-chunking-starts" tabindex="-1">When chunking starts <a class="header-anchor" href="#when-chunking-starts" aria-label="Permalink to “When chunking starts”">​</a></h2><p>Chunking is enabled with the <code>fileChunking</code> option. A file is chunked only when its size is greater than <code>threshold</code>.</p><div class="language-tsx"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { useCourier } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;use-courier&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">addFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useCourier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  url: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/api/uploads&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  fileChunking: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    route: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/api/uploads/chunks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    threshold: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1024</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1024</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    chunkSize: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1024</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1024</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    maxChunkRetries: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Files at or below the threshold use the regular <code>url</code> endpoint. Larger files use <code>fileChunking.route</code> instead.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h2><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>route</code></td><td><code>string</code></td><td>Endpoint that receives each chunk.</td></tr><tr><td><code>threshold</code></td><td><code>number</code></td><td>File size in bytes at which chunking begins.</td></tr><tr><td><code>chunkSize</code></td><td><code>number</code></td><td>Size of each chunk in bytes. Defaults to <code>threshold</code>.</td></tr><tr><td><code>maxChunkRetries</code></td><td><code>number</code></td><td>Additional attempts for a failed chunk. Defaults to <code>2</code>.</td></tr></tbody></table><p>All sizes are measured in bytes. For example, <code>10 * 1024 * 1024</code> represents 10 MiB.</p><h2 id="how-a-chunked-upload-works" tabindex="-1">How a chunked upload works <a class="header-anchor" href="#how-a-chunked-upload-works" aria-label="Permalink to “How a chunked upload works”">​</a></h2><p>For a file with a size of 12 MiB and a <code>chunkSize</code> of 5 MiB, the hook sends three requests:</p><ol><li>Chunk <code>0</code>: bytes <code>0</code> through <code>5 MiB</code></li><li>Chunk <code>1</code>: bytes <code>5 MiB</code> through <code>10 MiB</code></li><li>Chunk <code>2</code>: the remaining <code>2 MiB</code></li></ol><p>The requests are sent one at a time. Each request uses <code>multipart/form-data</code> and includes:</p><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>file</code></td><td>The current chunk as a file part.</td></tr><tr><td><code>uploadId</code></td><td>A unique ID shared by every chunk in this upload.</td></tr><tr><td><code>chunkIndex</code></td><td>The zero-based index of the current chunk.</td></tr><tr><td><code>totalChunks</code></td><td>The total number of chunks for the file.</td></tr></tbody></table><p>The final chunk response becomes the <code>data</code> value returned by <code>addFile</code> or <code>retryUpload</code>.</p><h2 id="progress-and-retries" tabindex="-1">Progress and retries <a class="header-anchor" href="#progress-and-retries" aria-label="Permalink to “Progress and retries”">​</a></h2><p>Progress is reported as the total number of bytes sent across all chunks, not just the current chunk. This means the <code>uploadProgress</code> value continues smoothly from one chunk to the next.</p><p>If a chunk fails, the hook retries that chunk up to <code>maxChunkRetries</code> additional times before marking the upload as failed. Cancellation is not retried. Calling <code>removeFile</code> aborts the active request and removes the file from the tracked list.</p><h2 id="server-responsibilities" tabindex="-1">Server responsibilities <a class="header-anchor" href="#server-responsibilities" aria-label="Permalink to “Server responsibilities”">​</a></h2><p>The chunk endpoint must:</p><ol><li>Read the <code>file</code>, <code>uploadId</code>, <code>chunkIndex</code>, and <code>totalChunks</code> fields.</li><li>Store each chunk under its <code>uploadId</code> and <code>chunkIndex</code>.</li><li>Detect when all chunks for an upload have arrived.</li><li>Reassemble the chunks in index order.</li><li>Return the completed upload response from the final request.</li></ol><p>The client does not reassemble the file. Your server must also clean up incomplete or expired uploads so abandoned chunks do not accumulate indefinitely.</p><h2 id="choosing-chunk-sizes" tabindex="-1">Choosing chunk sizes <a class="header-anchor" href="#choosing-chunk-sizes" aria-label="Permalink to “Choosing chunk sizes”">​</a></h2><p>A larger chunk size means fewer requests and less request overhead, but each retry sends more data. A smaller chunk size can make retries cheaper and may work better with request-size limits, but it creates more requests.</p><p>Choose a <code>threshold</code> and <code>chunkSize</code> that fit your server&#39;s request limits and storage strategy.</p>`,25)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};